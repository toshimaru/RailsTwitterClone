<%= form_with model: current_user.active_relationships.find_by(followed: user), html: { method: :delete } do |form| %>
  <%= form.hidden_field :follower_id  %>
  <%= form.hidden_field :followed_id  %>
  <%= form.submit "Unfollow", class: "btn btn-large btn-secondary" %>
<% end %>
