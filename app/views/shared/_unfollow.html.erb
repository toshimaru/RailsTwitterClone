<%= form_with model: current_user.active_relationships.find_by(followed: @user), local: true, html: { method: :delete } do |form| %>
  <%= form.submit "Unfollow", class: "btn btn-large btn-default" %>
<% end %>
