.profile.clearfix
  = link_to gravatar_for(current_user, size: 52), current_user
  div = link_to current_user.name, current_user

.stats
  = render 'shared/stats'

.tweet-box
  = render 'shared/tweet_form'
